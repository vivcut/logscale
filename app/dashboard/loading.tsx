/**
 * Dashboard route loading state. Returns nothing visible — the actual content
 * fades in via opacity transition (see page-transition.tsx) once loaded.
 */
export default function DashboardLoading() {
 return null;
}
